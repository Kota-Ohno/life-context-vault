# Copy to deploy/relay/compose.env before docker compose up.
# Caddy uses this file only for HTTPS certificate issuance and routing.
LCV_RELAY_PUBLIC_HOST=relay.example.com
LCV_RELAY_ACME_EMAIL=ops@example.com
