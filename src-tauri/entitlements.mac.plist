<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  macOS entitlements for the signed/notarized Life Context Vault build.

  Hardened-runtime compatible. The app needs:
    - network client: it connects to the managed MCP relay and AI providers.
    - network server: the optional local MCP relay binds a loopback port.
    - JIT / unsigned-executable-memory / disable-library-validation: required by
      the Tauri/WebKit webview, and because the four sidecar binaries
      (lcv-mcp/lcv-relay/lcv-agent/lcv-capture-host) are signed as separate
      Mach-O images embedded as resources.

  Keychain access uses the `security` CLI against the user login keychain, so no
  keychain-access-groups entitlement is required. Revisit if the app moves to
  the Keychain Services API or App Sandbox.
-->
<plist version="1.0">
<dict>
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.network.client</key>
  <true/>
  <key>com.apple.security.network.server</key>
  <true/>
</dict>
</plist>
