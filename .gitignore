node_modules/
dist/
.DS_Store
npm-debug.log*
browser-extension/native-host.dev.json
src-tauri/binaries/
deploy/relay/relay.env
deploy/relay/compose.env

# SDD subagent scratch (briefs, reports, diffs, progress ledger)
.superpowers/
docs/superpowers/
docs/public-release-progress.md

# Session/tool scratch (never commit)
.playwright-mcp/
.serena/
.takt/
*.png

# Local Claude Code settings (machine-specific, never commit).
# Glob also covers macOS Finder duplicates like "settings.local 2.json".
.claude/settings.local*.json
